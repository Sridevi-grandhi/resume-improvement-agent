package com.resume.agent;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the Resume Improvement Agent backend.
 * SpringBootApplication annotation enables auto-configuration and component scanning.
 */
@SpringBootApplication
public class ResumeImprovementAgentApplication {

    public static void main(String[] args) {
        SpringApplication.run(ResumeImprovementAgentApplication.class, args);
    }
}
